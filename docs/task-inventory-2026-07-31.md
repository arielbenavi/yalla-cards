# מלאי משימות מקובץ לפי נושא — 2026-07-31

כל 16 הפתקים הפתוחים + הפריטים התלויים מה-handoff, מקובצים לפי הפיצ'ר שהם נוגעים בו.
לכל נושא: מה המצב בפועל בקוד/DB, מה אפשר לעשות מיד, ומה דורש החלטה.

**סימונים:** 🟢 אפשר לבצע עכשיו בלי להתייעץ · 🟡 צריך chatifai · 🔴 צריך החלטה/מחקר

---

## A. תזמון FSRS וחזרה יומית

| פתק | תוכן |
|---|---|
| `a8d25fe5` | "למה אני רואה ג'נין/חיפא/בן/בת בחזרה יומית? זה מפגש ראשון ואנחנו במפגש רביעי" |
| `ac2ed4f2` | אימון ממוקד 15-30 דק' על "שוב"/"קשה", משוקלל לשיעורים אחרונים; למה זה לא בסדר אקראי |

### מה מצאתי בפועל

מדדתי את מצב ה-DB עכשיו:

```
850 card_srs · 597 חדשים (state=New) · 253 בחזרה (state=Review) · 0 due כרגע
חדשים לפי שיעור:  שיעור 3: 306 | מפגש 4: 162 | שיעור 2: 78 | ללא שיעור: 30
                  שיחה אמיתית: 10 | שיעור 1: 6 | מפגש בעפ 1: 5
```

**התשובה ל-`a8d25fe5`:** אין באג ב-FSRS. `app/api/review/queue/route.ts:75` מושך כרטיסים חדשים
ב-`ORDER BY due ASC`, ו-`due` של כרטיס חדש = זמן ההוספה. נשארו 6 כרטיסים חדשים משיעור 1
שיושבים בראש התור, ולכן הם נכנסים ראשונים לפני כל השאר. ב-12 כרטיסים ליום, 597 כרטיסים חדשים
= כ-50 יום עד שהתור ייגמר.

**הערה על `mode=all`:** ה-`CLAUDE.md` מתעד `ORDER BY due ASC`, אבל
[route.ts:36](app/api/review/queue/route.ts:36) עושה `.sort(() => Math.random() - 0.5)` אחרי
המיון — כלומר החזרה הכללית **כן** אקראית, בניגוד למתועד. זה כנראה מה שהפתק מתלונן עליו.
צריך להחליט מה הכוונה ואז ליישר בין הקוד לתיעוד.

### הצעות

🟢 **תעדוף שיעור לכרטיסים חדשים.** להוסיף `lesson_priority` (או פשוט למיין לפי
`lessons.date DESC` ואז `due ASC`) בענף הכרטיסים החדשים בלבד. שינוי קריאה בלבד, לא נוגע
ב-`card_srs` — עומד בכלל ההפיכות. ~15 שורות.

🔴 **אימון ממוקד (`ac2ed4f2`)** — זה עיצוב, לא באג. הכנתי פרומפט מחקר (נספח 1).
השאלות הפתוחות: כמה כרטיסים בישיבה של 20 דק'? האם "אימון ממוקד" צריך לכתוב ל-FSRS או
להיות read-only כמו שאר הדרילים? (הכלל שלך אומר read-only — וזה מגביל את העיצוב.)

---

## B. פעלים והטיות

| מקור | תוכן |
|---|---|
| בקשה שלך (#1) | לעבור על כל המילים, לזהות פעלים, ולהביא מ-chatifai את כל ההטיות |
| `04cff308` | מפרט מסך הטיות (Processing Instruction + retrieval) |
| `5774ff07` | תרגול הַאדִי / הַאדַאכּ / הַדִיכּ / הַדוֹל |
| `5e4b20e5` | "נטיות לא טוב" |

### מה כבר קיים — וזו הפתעה

**`verb_conjugations` כבר קיימת** (`supabase/migrations/0012`), עם `conjugation_srs` (3 מסלולי
SRS לכל פועל) ו-10 פעלים מוזרעים ב-`0013`. `app/api/inflections/` משתמש בהן. אבל:

- ה-`forms` מכילות **רק הווה** (b-imperfect), 8 גופים. אין עבר, אין ציווי, אין שם פועל.
- הצורות המוזרעות הן **ערבית ספרותית ולא פלסטינית** — `بقرأ` / `بتقريئي` לא נאמרות בפלסטינית.
- אין תעתיק עברי בכלל. כל האפליקציה עובדת בתעתיק עברי מנוקד; הטבלה הזו לא.
- אין `chatifai_verified`.

ה-handoff הציע טבלה **חדשה** `verb_paradigms`. אני חושב שזו טעות: זה ישאיר טבלה שבורה
שה-`/inflections` הקיים ממשיך לקרוא ממנה. עדיף להרחיב את `verb_conjugations` (להוסיף
`root_translit`, `meaning_he` כבר יש, `forms` להרחיב למבנה `{past:{}, present:{}, imperative:{}}`
עם `{translit, arabic}` בכל תא, ו-`chatifai_verified`) ולהחליף את ההזרעה.

### הפעלים שזיהיתי בכרטיסים (17 שורשים)

| שורש | מופיע ככרטיס | שיעור |
|---|---|---|
| راح | רַאח · רֻחֵת · רֻחְתִי · רֻחְנַא · רוּחְנַא | מפגש בעפ 2 |
| رجع | אַרְגַ'ע · רִגִ'עֵת | מפגש בעפ 2 |
| حكى | בַּחְכִּי · בִּחְכִּי · אַחְכִּי · בִּחְכּוּ · אַחְכִּי-לַכּ · חַכִּי | מפגש בעפ 1/2 |
| حبّ | בַּחִבּ · בִּתְחִבּ | מפגש בעפ 2 |
| أخذ | אַחַ'דֵת | מפגש בעפ 2 |
| شاف | שֻפְת · בִּנְשׁוּף | שיעור 2 / מפגש בעפ 2 |
| أعطى | אַעְטַא | שיעור 2 |
| طلع | אִטְלַע · עִיטְלַע | שיעור 1 |
| اشتغل | בַּשְתְעֶ׳ל | מפגש בעפ 1 |
| وقّف | וַקֶּף | מפגש בעפ 1 |
| اتعلّم | בִּתְעַלַּם | מפגש בעפ 2 |
| كمّل | כַּמֵּל | מפגש בעפ 2 |
| فاز | יְפוּז | מפגש בעפ 2 |
| أكل | תָאכֻּל | מפגש בעפ 2 |
| فهم | פַאהֶם (בינוני) | שיעור 1 |
| لعن | יִלְעַן | שיעור 2 |
| حكم | חַכַּם | שיעור 1 |

🟡 **מה שאני מציע לעשות:** להריץ chatifai על 17 השורשים, לקבל טבלת הטיות מלאה בתעתיק עברי
מנוקד (עבר · הווה · ציווי, 8 גופים), ולכתוב אותן ל-`verb_conjugations` עם `chatifai_verified=true`.
זה בערך 17 שאילתות chatifai. אני יכול להתחיל מיד — **אבל תאשר קודם את ההחלטה להרחיב את
הטבלה הקיימת במקום ליצור חדשה**, כי זה נוגע במסך `/inflections` שכבר עובד.

🔴 **`5774ff07` (כינויי רמז)** — כל 6 הצורות כבר קיימות ככרטיסים בשיעור 1
(הַאדַא/הַאדִי/הַדוֹל/הַדַאכּ/הַדִיכּ/הַדוֹלַאכּ). זה לא חוסר תוכן, זה חוסר **תרגול ניגודי** —
בדיוק מה ששלב 1 במפרט `04cff308` מתאר. הייתי מקפל את זה לתוך מסך ההטיות במקום לבנות משהו נפרד.

---

## C. זיהוי אותיות ערביות

`23486a6b` — זיהוי ערבית→עברית, דגש על אותיות מבלבלות, חיבורים וסופיות, עם סימון "לא זיהיתי".
אתה עצמך כתבת בפתק "אולי לחפש על זה בdeep research".

🔴 מסכים. הכנתי פרומפט (נספח 2). זה הפיצ'ר היחיד ברשימה שאין לו שום תשתית קיימת בקוד —
מתחילים מאפס, אז כדאי להתחיל נכון.

---

## D. איכות תוכן ואוצר מילים

| פתק | מצב |
|---|---|
| `db4e9d5e` | "הצלחה / תופיק — תעיף את הסלאש" · הכרטיס: `95d9be` תַוְפִיק |
| `6b4e5af0` | כרטיסיית כַּמַאן עם ההסבר מ-chatifai |
| `b4fe5996` | לחלץ מהביטויים/משפטים מילים שאין לנו |
| בקשה שלך (#5) | רוּחְנַא (חולם) מול רֻחְנַא (קובוץ) — שני כרטיסים, אותה משמעות |

🟢 **`db4e9d5e`** — עדכון שדה אחד. דקה.

🟢 **`6b4e5af0`** — כַּמַאן **כבר קיים** ככרטיס `c1a2fc` בשיעור 2 ("גם / עוד"). ההסבר שהעתקת
מ-chatifai לפתק צריך להיכנס ל-`notes` של הכרטיס הקיים, לא כרטיס חדש. דקה.

🟢 **`b4fe5996`** — יש כבר `scripts/find-missing-vocab-from-phrases.ts` לא-מקומיט. צריך להריץ,
לסקור את הפלט, ואז 🟡 להעביר את המילים החדשות דרך chatifai לניקוד לפני ההכנסה.

🟡 **הכפילות רֻחְנַא** — צריך לשאול את chatifai מה הניקוד הנכון ל"הלכנו", ואז למחוק את השגוי.
שים לב: אם לשניהם יש היסטוריית `card_srs`, מחיקה תמחק גם היסטוריית חזרות. אני אבדוק ואשאיר
את הכרטיס עם ההיסטוריה העשירה יותר.

---

## E. שירים

`efbd5595` — כל השירים בפורמט אחיד: **תעתיק + תרגום עברי קודם, ערבית אחר כך. בלי אנגלית.**
וגם הסקריפט שאוסף שירים צריך לייצר את הפורמט הזה.

### מה מצאתי ב-DB (8 שירים)

| שיר | שורות | מצב |
|---|---|---|
| YAMA · Zidi · Tamally Maak · Tisma'ani · דבקה בת"א · Allah Aleik | תקין | `line` = תעתיק עברי, `words[]` מלא |
| **Immer** | 46 | ❌ `line` = **ערבית גולמית** במקום תעתיק · **28 מתוך 46 שורות עם `words: []`** |
| **سوولنا كاسة شاي** | 35 | ❌ שדה `ar` מכיל **תעתיק עברי** במקום כתב ערבי · הכותרת בערבית |

**`Allah Aleik Ya Seedy` כבר הוכנס** (47 שורות) — ה-handoff טעה כשרשם שהסקריפט לא נכתב.

🟢 לתקן את `سوولنا`: הכותרת והשדות `ar` — עבודה מכנית, בלי chatifai.
🟡 לתקן את Immer: 28 שורות צריכות תעתיק + תרגום מילה-במילה.
⚠️ **אזהרה:** Immer הוא ב**דארג'ה מרוקאית**, לא פלסטינית. chatifai הוא קורס ערבית פלסטינית —
התשובות שלו על מרוקאית עלולות להיות לא אמינות. שווה לשקול מקור אחר לשיר הזה בלבד.
🟢 להוסיף בדיקת סכמה לסקריפט האיסוף כך שהבעיה לא תחזור.

---

## F. סימולציית שיחה ודו-שיחים

| מקור | תוכן |
|---|---|
| `a4e6b161` | להזין את כל ה-JSON של הכרטיסים למודל חזק, לגזור אבני בניין לסוגי שיחות, כולל הדו-שיחים מהספר |
| בקשה שלך (#3) | UI לדו-שיחים של מפגשים 1-2 |

**מצב:** דו-שיחים 3 ו-4 יושבים ב-`paradigms` (`slug="translation_sentences"`, 20+11 משפטי
תרגום עברית→ערבית). אין להם מסך. `app/api/simulate/route.ts` מריץ 5 תרחישים על Haiku.

⚠️ **התנגשות עם גבול קיים:** בזיכרון רשום שהוצאת מהסקופ "יצירת משפטים מהמילים שלי" באמצעות
מודל. `a4e6b161` מבקש בדיוק לגזור תוכן מהכרטיסים דרך מודל. ההבדל שאני מזהה: שם דובר על
**יצירת כרטיסי לימוד חדשים**, וכאן על **הרכבת תרחישי שיחה** מתוכן שכבר אימתת. אם זו הכוונה —
תגיד ואמשיך. אם לא — נסגור את הפתק.

🔴 בכל מקרה זה צריך החלטה על **מנגנון התרגול**: הפתק מונה שלוש אפשרויות (בחירה מרובה /
קריאה מהתעתיק ופירוש / משהו אחר) בלי להכריע. פרומפט מחקר בנספח 3.

---

## G. הקלטות

| פתק | מצב |
|---|---|
| `c87bcd44` | "תמלל הכל" — **בוצע חלקית** |
| `5026562b` | עריכת טווחי הקלטה מתוך מסך החזרה, לאדמין, אייקון קטן |

**`c87bcd44` — שים לב, יש חלק שלא בוצע.** הפתק מבקש שני דברים: (א) כפתור "תמלל הכל" ✅ נבנה
בסשן הקודם; (ב) **"שיפעל כל פעם שאני מעלה הקלטות"** ❌ לא נבנה. אל תסגור את הפתק עדיין.

🟢 חלק (ב) הוא שינוי קטן ב-`app/recordings/page.tsx` — לקרוא ל-`transcribeOne` בסיום העלאה.

🟢 **`5026562b`** — הרכיב לעריכת טווחים כבר קיים (commit `cbda3d6`, "recording range edit").
צריך רק לחשוף אותו ממסך החזרה מאחורי בדיקת אדמין. עבודת UI קטנה.

---

## H. מקורות מדיה חיצוניים

🔴 **`5e76460f` YouTube** — חסום עליך: צריך `YOUTUBE_API_KEY` מ-Google Cloud Console
(YouTube Data API v3, חינם עד 10,000 יחידות ביום). ברגע שהמפתח ב-`.env.local` וב-Vercel,
השינוי בקוד קטן ונוגע בקובץ אחד. **זה הפריט היחיד ברשימה שאני חסום בו לגמרי.**

🔴 **`5a28521e` פיד סושיאל** — ממליץ לא לבנות. אין API ציבורי למשוך רילים של חשבון אחר;
הטמעה נותנת iframe בלי תמלול ובלי חילוץ אוצר מילים, כלומר רשימת לינקים. הערך האמיתי
(תמלול + חותמות זמן + חיתוך לכרטיס) **כבר קיים** ב-`/recordings`. הצעה: לנסח מחדש את הפתק
ל"להקל על הכנסת אודיו מרילים שמורים לתוך /recordings".

🔴 **`6a3d8964` Offline/PWA** — ממליץ לדחות. תור כתיבה offline הוא הדבר המסוכן ביותר ברשימה
ל-FSRS: ציונים שמשוחזרים עם חותמות זמן שגויות מקלקלים את התזמון. בנוסף כתובות האודיו החתומות
פגות אחרי 10 דקות. חלופה זולה: **offline לקריאה בלבד** — cache של הכרטיסים בלי דירוג כלל.
כחמישית מהעבודה, אפס סיכון.

---

## I. משחק תמונות

`5e4b20e5` (החצי השני) — תמונות ברמת כיתה א' לפי אוצר המילים, בקלאסטרים.

🔴 שתי שאלות פתוחות שלא אחליט בשבילך: מאיפה התמונות (אתה אמרת בעבר "תמונות שאני מספק בלבד"),
ואיך מקבצים 539 מילים לקלאסטרים ויזואליים. פרומפט מחקר בנספח 4.

---

# מה אני מציע לעשות עכשיו

**מסלול מהיר — כולו 🟢, בלי שאלות אליך, בערך שעה:**

1. `db4e9d5e` — הסלאש בתופיק
2. `6b4e5af0` — הסבר כַּמַאן ל-`notes` של הכרטיס הקיים
3. `c87bcd44` חלק (ב) — תמלול אוטומטי בסיום העלאה, ואז סגירת הפתק
4. `5026562b` — חשיפת עריכת טווחים במסך החזרה לאדמין
5. `a8d25fe5` — תעדוף שיעור לכרטיסים חדשים + סגירת הפתק
6. `سوولنا كاسة شاي` — תיקון שדות ה-`ar` והכותרת

**אחרי זה, המשימה הגדולה:** הפעלים (B). זו הבקשה המפורשת שלך והיא הכי גדולה בהשפעה —
אבל היא צריכה החלטה אחת ממך: להרחיב את `verb_conjugations` או לבנות טבלה חדשה.

**במקביל:** תריץ את 4 פרומפטי המחקר ותחזור עם התשובות.

---

# נספחים — פרומפטים ל-ChatGPT Deep Research

## נספח 1 — אימון ממוקד ב-FSRS (`ac2ed4f2`)

> אני בונה אפליקציית spaced repetition אישית ללימוד ערבית פלסטינית מדוברת, מבוססת ts-fsrs
> (אלגוריתם FSRS-5). כרגע יש בה 850 כרטיסים: 597 עוד לא נלמדו, 253 בשלב חזרה, מגבלה של 12
> כרטיסים חדשים ביום.
>
> אני רוצה להוסיף **מצב "אימון ממוקד"** — ישיבה של 15-30 דקות שמתמקדת בכרטיסים שדירגתי
> "שוב" (Again) או "קשה" (Hard), עם העדפה למילים מהשיעורים האחרונים.
>
> אנא חקור ותן לי המלצות מבוססות-מחקר על:
> 1. **גודל ישיבה.** מה הספרות אומרת על מספר הפריטים המקסימלי שאפשר לתרגל ביעילות בישיבה
>    אחת של 20-30 דקות בלמידת אוצר מילים בשפה שנייה? מה קורה מעבר לזה (עייפות, ירידה בשימור)?
> 2. **בחירת פריטים.** איך צריך לשקלל בין (א) כרטיסים קשים/נכשלים, (ב) עדכניות השיעור,
>    (ג) קרבה למועד ה-due של FSRS? יש נוסחאות או היוריסטיקות מתועדות מ-Anki / SuperMemo /
>    מחקר lapse-driven review?
> 3. **חזרה תוך-ישיבתית.** כשפריט נכשל בתוך הישיבה, כמה פעמים ומתי הוא צריך לחזור באותה
>    ישיבה? (expanding retrieval vs. uniform spacing — מה המחקר מראה?)
> 4. **הבעיה הקריטית: זיהום נתונים.** אני **לא** רוצה שהאימון הממוקד יכתוב לטבלאות
>    ה-FSRS — הן מקודשות. איזה ערך לימודי נשאר לתרגול חוזר שלא מעדכן תזמון? האם מחקר על
>    "testing effect" תומך בתרגול אחזור גם בלי עדכון תזמון? האם יש דפוסים מתועדים לתרגול
>    מקביל שלא מזהם את מודל התזמון הראשי?
> 5. **סדר ההצגה.** האם צריך להיות אקראי, מקובץ לפי שיעור, או interleaved? מה הראיות על
>    interleaving לעומת blocking בלמידת אוצר מילים?
>
> תן תשובות קונקרטיות עם מספרים ומקורות, לא עקרונות כלליים. אני צריך להפוך את זה
> לאלגוריתם בחירה.

## נספח 2 — זיהוי אותיות ערביות (`23486a6b`)

> אני בונה מודול ללימוד קריאת **כתב ערבי** בתוך אפליקציית SRS אישית. הלומד דובר עברית,
> כבר יודע כ-800 מילים בערבית פלסטינית מדוברת — אבל **רק בתעתיק עברי מנוקד**. הוא לא
> קורא כתב ערבי בכלל. המטרה: זיהוי אותיות ערביות, במיוחד (א) אותיות שנבדלות רק בנקודות,
> (ב) הצורות המחוברות (initial/medial/final/isolated), (ג) סופיות מילים.
>
> אנא חקור ותן לי:
> 1. **סדר הצגה.** מה הסדר המומלץ במחקר להצגת האלפבית הערבי ללומדים מבוגרים? לפי תדירות?
>    לפי דמיון ויזואלי? לפי קלות הגייה? יש קונצנזוס בספרות של Arabic-as-a-foreign-language?
> 2. **אותיות מבלבלות.** אילו זוגות/שלשות ערביות הכי מבולבלות אמפירית אצל מתחילים
>    (ب/ت/ث, ج/ح/خ, د/ذ, ر/ز, س/ش, ص/ض, ط/ظ, ع/غ, ف/ق)? יש נתוני שגיאות ממחקר?
> 3. **יתרון וחיסרון של רקע עברי.** הלומד יודע קרוא-וכתוב בעברית. אילו transfer effects
>    חיוביים ושליליים מתועדים בין קריאת עברית לקריאת ערבית? (שתיהן מימין לשמאל, שתיהן
>    שמיות, אבל החיבוריות שונה לגמרי.)
> 4. **חיבוריות.** איך מלמדים ביעילות את ארבע צורות האות? יש ראיות שעדיף ללמד את כל ארבע
>    הצורות יחד לכל אות, או ללמד תחילה את הצורה המבודדת ואז את המחוברות?
> 5. **עיצוב התרגיל.** לזיהוי אותיות — מה עדיף: בחירה מרובה, התאמה, הקלדה, או discrimination
>    ("האם שתי האותיות האלה זהות?")? יש השוואות ישירות במחקר?
> 6. **סימון "לא זיהיתי".** אני רוצה שהלומד יסמן אותיות ספציפיות שלא זיהה בתוך מילה.
>    יש דפוסי UI מתועדים לזה במערכות לימוד כתב (יפנית/סינית/ערבית)?
>
> תן המלצות ישימות עם מקורות. אני צריך להפוך את זה למפרט מסך.

## נספח 3 — תרגול שיחה מאוצר מילים קיים (`a4e6b161`)

> יש לי אפליקציית SRS אישית לערבית פלסטינית עם כ-820 פריטים מאומתים: 539 מילים, 138 ביטויים,
> 143 משפטים — כולם בתעתיק עברי מנוקד עם תרגום. בנוסף יש דו-שיחים מספר לימוד ורשימות משפטי
> תרגום עברית→ערבית.
>
> אני רוצה לבנות תרגול שיחה שמשתמש **רק בתוכן שכבר אימתתי** — בלי לייצר משפטים חדשים במודל,
> כי זה מסכן את דיוק הניב.
>
> אנא חקור:
> 1. **מה עובד לתרגול שיחה בלי בן-שיח אנושי?** מה הראיות על substitution drills, dialogue
>    completion, role-play מובנה, ו-shadowing — לרכישת שטף בדיבור בשפה שנייה?
> 2. **בחירה מרובה מול הפקה.** הפתק שלי מונה שלוש חלופות: בחירת התשובה המתאימה מרשימה /
>    קריאה מהתעתיק וניסיון לפרש / הפקה חופשית. מה המחקר אומר על היחס בין recognition
>    ל-production בתרגול שיחה? באיזה שלב עוברים מאחד לשני?
> 3. **הרכבת דו-שיח מפריטים מאומתים.** יש שיטות מתועדות לבנות עצי שיחה מתוך מלאי סגור של
>    ביטויים, בלי יצירת שפה חדשה? (dialogue templates, frame-and-slot, conversation scaffolds)
> 4. **דו-שיחים מספר לימוד.** מה הדרך היעילה ביותר לתרגל דו-שיח כתוב מספר — קריאה בקול,
>    השלמת חסרים, תרגום דו-כיווני, שחזור מהזיכרון? יש השוואות?
> 5. **ערבית מדוברת ספציפית.** יש שיקולים מיוחדים לתרגול ניב מדובר (פלסטיני) לעומת ערבית
>    ספרותית — במיוחד ללומד שקורא רק תעתיק ולא כתב ערבי?
>
> תן המלצות קונקרטיות לעיצוב מסך, עם מקורות.

## נספח 4 — משחק תמונות ואוצר מילים ויזואלי (`5e4b20e5`)

> יש לי אפליקציית SRS אישית לערבית פלסטינית עם 539 מילים בתעתיק עברי מנוקד. אני רוצה לבנות
> משחק מבוסס-תמונות: תמונת סצנה + גרירת מילים לאזורים בתמונה. הכוונה שלי היא תמונות ברמת
> ספר לימוד של כיתה א'.
>
> אנא חקור:
> 1. **למידה ויזואלית של אוצר מילים.** מה הראיות על picture-word association לעומת
>    translation pairs לשימור לטווח ארוך? באילו קטגוריות מילים תמונות עוזרות ובאילו לא
>    (מוחשי מול מופשט)?
> 2. **סצנה מול פריט בודד.** האם עדיף תמונה אחת עם הרבה פריטים (סצנת מטבח, סצנת כיתה)
>    או תמונה אחת לכל מילה? מה המחקר על context-rich vs. isolated picture cues?
> 3. **קיבוץ לקלאסטרים.** איך הכי נכון לקבץ אוצר מילים לסצנות? יש טקסונומיות מתועדות
>    (semantic fields, thematic clusters) ומה הראיות שקיבוץ סמנטי דווקא **מפריע** ללמידה
>    (interference בין מילים דומות)?
> 4. **מקורות תמונות.** מהם מאגרי התמונות החינמיים והחוקיים לשימוש חינוכי שמתאימים לסצנות
>    אוצר-מילים? האם תמונות מיוצרות-AI מתאימות פדגוגית, ומה הבעיות המתועדות שלהן
>    (חפצים לא-קוהרנטיים, הטיה תרבותית)?
> 5. **התאמה תרבותית.** התוכן הוא ערבית פלסטינית. כמה חשוב שהתמונות ישקפו הקשר תרבותי
>    מתאים (בית, אוכל, לבוש, רחוב) ולא סטוק מערבי גנרי? יש ראיות שזה משפיע על שימור?
>
> תן המלצות ישימות עם מקורות ורשימת מאגרים קונקרטית.
