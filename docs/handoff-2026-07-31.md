# Handoff — session 2026-07-31

This document picks up from the context-limit cutoff on 2026-07-31.
Read `CLAUDE.md` and `AGENTS.md` before touching anything.

---

## What was done this session

### Recordings page (`app/recordings/page.tsx`) — full rewrite
- **Removed** static "ללא תמלול" badge (it was always showing, even when transcription was in progress)
- **Added** `transcribingIds: Set<string>` state — shows "בתהליך תמלול" badge only during active transcription
- **Added** title input in the upload form (auto-fills from filename on file select)
- **Added** `transcribeOne(id)` function: fetches audio URL → downloads blob → POST transcribe-chunk → PATCH save-transcript
- **Added** `transcribeAll()` function: loops untranscribed recordings sequentially
- **Added** "תמלל הכל (N)" button — visible to admins when untranscribed recordings exist

Companion files also updated:
- `lib/recording-upload.ts` — added `title?: string | null` option
- `app/api/recordings/route.ts` — POST now accepts and saves `title`

**If the user says "תמלל הכל" button is missing:** they must be logged in as admin (`/login` → "כניסת אדמין"). The button appears inside the admin panel, only when `untranscribedCount > 0`.

### מפגש בעפ 2 — created and populated
- Lesson created: `{ date: "2026-07-31", title: "מפגש בעפ 2" }`, ID: `1ec690db-bde3-463b-9c27-888150347a75`
- **74 cards moved** from שיעור 2 → מפגש בעפ 2:
  - 64 cards created 2026-07-29 at ~20:16 (the big chatifai batch)
  - 10 cards created 2026-07-30 (a second chatifai batch)
  - The July-28 batch (20 cards, location-sentence patterns for שיעור 2) was left in שיעור 2 intentionally
- **8 missing cards inserted** (chatifai_verified=true):
  - כַּאן בִּדַּכּ (האם רצית?)
  - דִיכּ הִנְדִי (תרנגול הודו)
  - בִּנַפְס אִל-הַוַא סַוַא (באותו מצב)
  - אַחַ'דֵת (לקחתי — "רוחמה" doesn't exist, chatifai confirmed)
  - רַאח (הלך/ילך)
  - רֻחֵת (הלכתי/הלכת זכר)
  - רֻחְתִי (הלכת נקבה)
  - רֻחְנַא (הלכנו)

**Possible duplicate:** DB may have both רוּחְנַא (cholam, old card moved from שיעור 2) and רֻחְנַא (kibbutz, newly inserted). Minor cleanup if needed.

### Daily tips (`lib/tips.ts`) — two updates
1. **New tip `id: "yaani"`** — יַעְנִי as the main Palestinian Arabic filler word ("כאילו" of Arabic). Chatifai confirmed: it's the most-used filler, appears every other sentence.
2. **Updated tip `id: "walla"`** — clarified that وَالله is a real oath in Palestinian Arabic (NOT a casual filler like in Israeli Hebrew). The casual filler is יַעְנִי.

### Dialogues 3 & 4 — inserted into `paradigms` table
- `(meeting=3, slug="translation_sentences")` — 20 Hebrew→Arabic translation sentences
- `(meeting=4, slug="translation_sentences")` — 11 Hebrew→Arabic translation sentences (p. 296)
- Data format: `{ type: "translation_exercise", description: "...", sentences: [{num, hebrew, translit}] }`
- Script: `scripts/insert-dialogues-3-4.ts`

---

## Lesson IDs (permanent)

| Lesson | ID |
|---|---|
| שיעור 2 | `5325a7b7-82e9-446c-ac41-d913690d52dd` |
| מפגש בעפ 1 | `b3e16d4f-af1f-4376-9e6e-f2bfc07524b4` |
| מפגש בעפ 2 | `1ec690db-bde3-463b-9c27-888150347a75` |
| שיחה אמיתית | `386f461f-62b5-4128-9125-fd20916542b0` |

---

## Pending / next tasks

### Verb conjugations schema (user's explicit request)
User said: "פעלים זה אובייקט נפרד שצריך להביא את כל ההטיות. מצידי תעשה לאובייקט של פעלים סכמה אחרת כמו שיש לדו שיח."

Design agreed on (not yet implemented):
```sql
create table verb_paradigms (
  id uuid primary key default gen_random_uuid(),
  root_translit text not null,   -- "ר-ו-ח"
  root_arabic   text,            -- "ر-و-ح"
  meaning_he    text not null,   -- "הלך / ללכת"
  lesson_id     uuid references lessons(id),
  -- forms: {past: {ana: {translit, arabic}, inta: {...}, ...}, present: {...}}
  forms         jsonb not null,
  chatifai_verified boolean default false
);
```
Source verbs from מפגש בעפ 2 cards. Ask chatifai for complete conjugation tables. First verb: רוח (הלך) — past tense forms already in DB as individual cards, need to consolidate.

### Dialogue 3 & 4 UI
Sentences are now in `paradigms` table but there's no screen to practice them yet. The existing `/inflections` page pattern could be adapted, but the inflections rebuild spec (`docs/handoff-inflections-rebuild.md`) takes priority.

### Inflections rebuild (`04cff308`)
Spec at `docs/handoff-inflections-rebuild.md`. Status: not started. Prerequisite: practice section merge (may already be done — verify).

### Songs — "Allah Aleik Ya Seedy"
Chatifai analysis exists in chat history from a previous session. Script to insert lyrics never written. Template: `scripts/update-yama-lyrics.ts`.

### `יַעְנִי` tip — add to backlog note
Other 4 filler words chatifai mentioned (for future tips if desired):
- הֵלַּא — עכשיו/ובכן
- טֵיַּב — טוב/אוקיי
- מַא הוּ — הרי ש...
- אַל-מַטְ'בּוּט — בדיוק/האמת

---

## Security constraint (permanent, never override)
**No writes to `card_srs`, `review_log`, or `cards.self_score` from any drill. Not one.**

## chatifai rule (permanent)
Never substitute chatifai with another AI. Always send directly to chatifai via `mcp__claude-in-chrome` tools. If unavailable, stop and ask the user.
